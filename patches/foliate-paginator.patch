diff --git a/paginator.js b/paginator.js
index 7980c20..744e145 100644
--- a/paginator.js
+++ b/paginator.js
@@ -794,6 +794,7 @@ export class Paginator extends HTMLElement {
         const delta = this.#vertical ? dy : dx
         const element = this.#container
         const { scrollProp } = this
+        if (!this.#scrollBounds) return  // readflow: guard upstream undefined crash
         const [offset, a, b] = this.#scrollBounds
         const rtl = this.#rtl
         const min = rtl ? offset - b : offset - a
@@ -802,6 +803,7 @@ export class Paginator extends HTMLElement {
             element[scrollProp] + delta))
     }
     snap(vx, vy) {
+        if (!this.#scrollBounds) return  // readflow: guard upstream undefined crash
         const velocity = this.#vertical ? vy : vx
         const [offset, a, b] = this.#scrollBounds
         const { start, end, pages, size } = this
@@ -858,6 +860,7 @@ export class Paginator extends HTMLElement {
         // at this point I'm basically throwing `requestAnimationFrame` at
         // anything that doesn't work
         requestAnimationFrame(() => {
+            if (!this.#touchState) return  // readflow: guard upstream undefined crash
             if (globalThis.visualViewport.scale === 1)
                 this.snap(this.#touchState.vx, this.#touchState.vy)
         })
